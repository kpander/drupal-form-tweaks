- ui for configuration
- conf vars for configuration
- allow hook configuration also through conf vars so we don't have to
  add a tweaks module just to add some common form config definitions
- rename to form_trim

